{% extends 'base.html.jinja' %}

{% block title %}{{ publisher.name }}{% endblock %}

{% block content %}
<div class="rounded-[2rem] border border-slate-200 bg-white p-8 shadow-sm">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
        <h2 class="text-2xl font-semibold text-slate-900">{{ publisher.name }}</h2>
        <a href="{% url 'publisher_edit' publisher.id %}" class="rounded-lg border border-emerald-600 px-4 py-2 text-sm font-semibold text-emerald-700 hover:bg-emerald-50">Edytuj dane wydawcy</a>
    </div>

    <div class="mt-6 grid grid-cols-2 gap-6">
        <div>
            <p class="text-sm text-slate-600">Kraj</p>
            <p class="text-lg font-semibold text-slate-900">{{ publisher.get_country_display }}</p>
        </div>
        <div>
            <p class="text-sm text-slate-600">Rok założenia</p>
            <p class="text-lg font-semibold text-slate-900">{{ publisher.founded_year }}</p>
        </div>
        {% if publisher.website %}
        <div>
            <p class="text-sm text-slate-600">Strona internetowa</p>
            <p class="text-lg"><a href="{{ publisher.website }}" target="_blank" class="text-emerald-600 hover:text-emerald-800">{{ publisher.website }}</a></p>
        </div>
        {% endif %}
        {% if publisher.email %}
        <div>
            <p class="text-sm text-slate-600">E-mail</p>
            <p class="text-lg"><a href="mailto:{{ publisher.email }}" class="text-emerald-600 hover:text-emerald-800">{{ publisher.email }}</a></p>
        </div>
        {% endif %}
    </div>
</div>

<div class="mt-8 rounded-[2rem] border border-slate-200 bg-white p-8 shadow-sm">
    <h3 class="text-xl font-semibold text-slate-900">Książki wydawcy w mojej bibliotece</h3>
    {% if books %}
    <ul class="mt-6 space-y-3">
        {% for book in books %}
        <li><a href="{% url 'book' book.id %}" class="font-medium text-emerald-600 hover:text-emerald-800">{{ book.title }}</a></li>
        {% endfor %}
    </ul>
    {% else %}
    <p class="mt-6 text-slate-600">Ten wydawca nie ma książek w bibliotece.</p>
    {% endif %}
</div>
{% endblock %}
